apiVersion: v1
kind: Service
metadata:
  name: devops-Mega-project
  labels:
    app: devops-Mega-project 
spec:
  selector:
    app: devops-Mega-project

  ports:
    - port: 8080
      targetPort: 8080

  type: LoadBalancer
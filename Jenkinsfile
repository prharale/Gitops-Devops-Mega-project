pipeline {
    agent any

    environment {
              APP_NAME = "devops-mega-project"
    }
     stages{
        stage("Cleanup Workspace"){
            steps {
                cleanWs()
            }
}
